# Octomap Python Wrapper

A python wrapper over the colcon octomap package. Only supports `ColorOcTree`.

See `test_scripts/test_color_tree.py` and `test_scripts/test_color_pcld.py` for example usage.
